<view class="top-navigator">
  <view class="navigator-item" wx:key="" wx:for="{{topNavigator}}">
    <view style="height:{{topNavigator.length}}" open-type="switchTab" class="wx-title {{(index == curTab)? 'wx-curTab':''}}" bindtap="clickTab" data-index="{{index}}">{{item}}</view>

  </view>
</view>